# mexc_bot 环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改

# ============================================
# Web Admin API 服务配置
# ============================================
# Web Admin API 服务主机地址（用于跨进程事件通知）
# 开发环境默认: localhost
# 生产环境示例: your-api-server.com 或 10.0.0.100
WEB_ADMIN_HOST=localhost

# Web Admin API 服务端口
# 开发环境默认: 8000
# 生产环境示例: 80, 443, 或其他端口
WEB_ADMIN_PORT=8000

# ============================================
# 数据库配置
# ============================================
# 数据库文件路径（可选，默认使用项目根目录下的 data/mexe_bot.db）
# DATABASE_PATH=/path/to/your/database.db

# ============================================
# 交易配置
# ============================================
# API密钥等敏感信息（根据实际需要添加）
# 注意：不要将包含敏感信息的 .env 文件提交到版本控制系统

# ============================================
# 日志配置
# ============================================
# 日志级别（可选）
# LOG_LEVEL=INFO

# ============================================
# 使用说明
# ============================================
# 1. 复制此文件为 .env：cp .env.example .env
# 2. 根据你的部署环境修改相应的值
# 3. 确保 .env 文件不被提交到 git（已在 .gitignore 中配置）
#
# 开发环境示例：
# WEB_ADMIN_HOST=localhost
# WEB_ADMIN_PORT=8000
#
# 生产环境示例：
# WEB_ADMIN_HOST=api.yourcompany.com
# WEB_ADMIN_PORT=443
#
# Docker 环境示例：
# WEB_ADMIN_HOST=web-admin-service
# WEB_ADMIN_PORT=8000