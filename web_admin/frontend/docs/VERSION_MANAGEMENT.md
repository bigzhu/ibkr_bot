# 自动版本号管理系统

## 📋 概述

前端项目现在支持自动版本号管理,每次构建时都会自动递增版本号,便于确认是否正常更新.

## 🚀 使用方法

### 开发构建

```bash
npm run build
```

- 自动生成构建版本号(包含时间戳)
- 格式:`1.1.0-build.20250803-122634`

### 生产发布

```bash
npm run build:prod
```

- 自动递增 patch 版本号
- 格式:`1.1.1`, `1.1.2`, `1.1.3`...

### 手动版本管理

```bash
# 递增 patch 版本 (1.1.0 -> 1.1.1)
npm run version:patch

# 递增 minor 版本 (1.1.0 -> 1.2.0)
npm run version:minor

# 递增 major 版本 (1.1.0 -> 2.0.0)
npm run version:major

# 生成开发版本 (1.1.0 -> 1.1.0-dev.timestamp)
npm run version:dev

# 生成构建版本 (1.1.0 -> 1.1.0-build.timestamp)
npm run version:build
```

## 📊 版本号格式

### 生产版本

- **格式**: `major.minor.patch`
- **示例**: `1.1.0`, `1.2.0`, `2.0.0`

### 开发版本

- **格式**: `major.minor.patch-dev.timestamp`
- **示例**: `1.1.0-dev.1754224004`

### 构建版本

- **格式**: `major.minor.patch-build.YYYYMMDD-HHMMSS`
- **示例**: `1.1.0-build.20250803-122634`

## 🔧 版本信息显示

### 在代码中获取版本信息

```typescript
// 环境变量方式
const appVersion = process.env.APP_VERSION;
const buildTime = process.env.BUILD_TIME;
const gitHash = process.env.GIT_HASH;
const gitBranch = process.env.GIT_BRANCH;
```

### 使用版本信息组件

```vue
<template>
  <VersionInfo />
</template>

<script setup>
import VersionInfo from 'src/components/VersionInfo.vue';
</script>
```

## 📁 文件结构

```
frontend/
├── scripts/
│   └── version-bump.js          # 版本号管理脚本
├── src/
│   └── components/
│       └── VersionInfo.vue      # 版本信息显示组件
├── docs/
│   └── VERSION_MANAGEMENT.md    # 使用说明(本文件)
├── package.json                 # 包含版本号
└── quasar.config.ts            # 构建配置,注入版本信息
```

## 🛠️ 自动化流程

### 构建时流程

1. **运行版本脚本** → 更新 package.json 版本号
2. **读取 Git 信息** → 获取提交哈希和分支名
3. **注入环境变量** → 通过 quasar.config.ts 注入到前端
4. **显示构建信息** → 在控制台输出版本详情

### 版本号策略

- **开发构建** (`npm run build`):生成带时间戳的构建版本
- **生产发布** (`npm run build:prod`):递增 patch 版本号
- **功能发布**:手动使用 `version:minor`
- **重大更新**:手动使用 `version:major`

## 🎯 最佳实践

### 开发阶段

- 使用 `npm run build` 进行日常构建
- 版本号会自动包含构建时间戳,便于区分不同构建

### 发布阶段

- 功能完成后使用 `npm run build:prod`
- 会自动递增版本号并生成生产构建

### 版本管理

- **Bug 修复**:使用 `version:patch` (1.1.0 → 1.1.1)
- **新功能**:使用 `version:minor` (1.1.0 → 1.2.0)
- **重大变更**:使用 `version:major` (1.1.0 → 2.0.0)

## 📈 示例输出

### 构建时控制台输出

```
🚀 构建版本信息:
   版本: 1.1.0-build.20250803-122634
   Git: d34061f (main)
   构建时间: 2025/8/3 20:26:34

🔧 环境变量:
export APP_VERSION="1.1.0-build.20250803-122634"
export GIT_HASH="d34061f"
export GIT_BRANCH="main"
export BUILD_TIME="2025-08-03T12:26:34.523Z"
```

### 前端显示效果

- 开发版本:🟠 `v1.1.0-dev.1754224004`
- 生产版本:🔵 `v1.1.0`
- 构建版本:🟠 `v1.1.0-build.20250803-122634`

通过版本号可以清楚地知道当前运行的是哪个构建,便于问题追踪和更新确认.
